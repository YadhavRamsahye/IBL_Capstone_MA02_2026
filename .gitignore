# Environment secrets - NEVER commit these
.env

# Python
__pycache__/
*.pyc
*.pyo

# Virtual environment
.venv/
venv/

# YOLO model weights (large binary)
*.pt

# IDE
.vscode/
.idea/

# Stray backups and archives - never intended to be tracked
main copy.py
*.bak
*.zip
IBL_Capstone_MA02_2026/

# Overpass API response cache - regenerable, and re-fetching costs the
# shared free API nothing worth avoiding on a fresh checkout
tools/validate_camera_coords_cache/

# Dev email outbox (contains live reset links)
sent_emails/

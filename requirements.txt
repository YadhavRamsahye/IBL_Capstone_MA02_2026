# NOTE: ffmpeg must be installed separately and added to PATH
# Download: https://ffmpeg.org/download.html
# Windows:  https://www.gyan.dev/ffmpeg/builds/
# After installing, restart your terminal and verify with:
#   ffmpeg -version

fastapi==0.115.0
uvicorn==0.30.6
jinja2==3.1.4
python-multipart==0.0.9
itsdangerous==2.2.0
ultralytics
opencv-python-headless
requests
beautifulsoup4
lxml
selenium
webdriver-manager
numpy
imageio-ffmpeg
anthropic
python-dotenv

# Database layer: main.py imports sqlalchemy directly, database.py connects
# through the asyncpg driver, and seed_users.py hashes passwords with bcrypt.
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
bcrypt==5.0.0
